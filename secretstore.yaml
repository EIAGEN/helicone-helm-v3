    kind: SecretStore
  target:
    name: helicone-ai-gateway-secrets-temp
    type: Opaque
    creationPolicy: Owner
  data:
    - secretKey: openai_api_key
      remoteRef:
        key: helicone/ai-gateway
        property: openai_api_key
    - secretKey: anthropic_api_key
      remoteRef:
        key: helicone/ai-gateway
        property: anthropic_api_key
    - secretKey: gemini_api_key
      remoteRef:
        key: helicone/ai-gateway
        property: gemini_api_key
    - secretKey: helicone_api_key
      remoteRef:
        key: helicone/ai-gateway
--
    kind: SecretStore
  target:
    name: helicone-secrets-temp
    type: Opaque
    creationPolicy: Owner
  data:
    - secretKey: postgres-password
      remoteRef:
        key: helicone/database
        property: postgres-password
    - secretKey: access_key
      remoteRef:
        key: helicone/storage
        property: access_key
    - secretKey: secret_key
      remoteRef:
        key: helicone/storage
        property: secret_key
    - secretKey: minio-root-user
      remoteRef:
        key: helicone/storage
--
    kind: SecretStore
  target:
    name: helicone-web-secrets-temp
    type: Opaque
    creationPolicy: Owner
  data:
    - secretKey: BETTER_AUTH_SECRET
      remoteRef:
        key: helicone/web
        property: BETTER_AUTH_SECRET
    - secretKey: STRIPE_SECRET_KEY
      remoteRef:
        key: helicone/web
        property: STRIPE_SECRET_KEY
---
# Source: helicone-core/templates/external-secrets/secret-store.yaml
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: aws-secrets-manager
  namespace: helicone
  labels:
    helm.sh/chart: helicone-core-0.1.0
    app.kubernetes.io/name: release-name-helicone-core
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/version: "2025.05.23"
    
    app.kubernetes.io/managed-by: Helm
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  provider:
    aws:
      service: SecretsManager
      region: "us-west-2"
      auth:
        serviceAccount:
          name: "external-secrets"
